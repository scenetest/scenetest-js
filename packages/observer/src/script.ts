/**
 * Observer script export
 *
 * This module exports the observer as a bundled IIFE string,
 * useful for injecting into HTML (e.g., by the Vite plugin).
 */

import { observerScript } from './script.generated.js'

export { observerScript }
